import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color deepVoid = Color(0xff050505); // The new main background
  static const Color darkCard = Color(0xff0D1B2A); // Dialogs/Modals

  // Accents (Aurora Colors)
  static const Color royalBlue = Color(0xFF4361EE);
  static const Color deepPurple = Color(0xFF7209B7);
  static const Color electricPink = Color(0xFFF72585);
  static const Color vibrantOrange = Color(0xFFFF9F1C);
  static const Color tealGreen = Color(0xFF2EC4B6);
  static const Color dangerRed = Color(0xFFE63946);
  static const Color successGreen = Color(0xFF00E676);

  // Text
  static const Color textPrimary = Colors.white;
  static Color textSecondary = Colors.white.withOpacity(0.5);

  // Glass Effects
  static Color glassBorder = Colors.white.withOpacity(0.08);
  static Color glassFill = Colors.white.withOpacity(0.03);
}
